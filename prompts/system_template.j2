{# System-Level Agent Template #}
## Agent Role
{{ agent_role }}

**Current Path (from project root):** {{ agent_path }}

## Language Grammar
```
{{ lark_grammar }}
```

{% if available_commands %}
## Available Terminal Commands
{{ available_commands }}
{% endif %}

{% if language_examples %}
## Language Examples
{{ language_examples }}
{% endif %}

## Instructions
Return exactly **one** command line with NO surrounding prose, code fences, or extra formatting.
Inside the command, use escaped quotes (\") and `\n` only when they appear within a quoted argument (e.g., `"some text\n"`).
Ensure the response ends with a single newline character.